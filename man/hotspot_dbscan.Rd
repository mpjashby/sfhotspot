% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hotspot_dbscan.R
\name{hotspot_dbscan}
\alias{hotspot_dbscan}
\title{Identify hotspots using DBSCAN}
\usage{
hotspot_dbscan(
  data,
  eps = NULL,
  min_pts = 5,
  density_adjust = 2,
  hull = c("concave", "convex"),
  hull_ratio = 0.75,
  transform = TRUE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{data}{An \link[sf:sf]{sf::sf} object containing point geometries.}

\item{eps}{A single positive number specifying the DBSCAN neighbourhood
radius in the units of the analysis co-ordinate reference system (CRS).
If \code{NULL}, the radius is calculated automatically from the mean point
density within the convex hull of \code{data}.}

\item{min_pts}{A single integer specifying the minimum number of points in
an \code{eps} neighbourhood, including the point itself, for a point to be a
core point. The default is \code{5}.}

\item{density_adjust}{A single positive number controlling automatic
selection of \code{eps}. Ignored unless \code{eps = NULL}. A value of \code{1} (the
background-density reference) identifies neighbourhoods with approximately
at least the mean density within the convex hull of \code{data}; the default of
\code{2} corresponds to approximately twice that density.}

\item{hull}{The type of hull used to represent each cluster: \code{"convex"}
or \code{"concave"} (the default).}

\item{hull_ratio}{For concave hulls, a number from zero to one specifying the
fraction convex. Zero produces a maximally concave hull and one produces a
convex hull. The default is \code{0.75}. Ignored when \code{hull = "convex"}.}

\item{transform}{DBSCAN uses Euclidean distances and therefore requires a
projected CRS. If \code{TRUE} (the default), geographic input is transformed
automatically with \code{\link[=st_transform_auto]{st_transform_auto()}} before analysis and the result is
transformed back afterwards. If \code{FALSE}, geographic input produces an
error.}

\item{quiet}{If \code{TRUE}, suppress informative messages about automatically
selected parameters and geometry preparation.}

\item{...}{Further arguments passed to \code{\link[dbscan:dbscan]{dbscan::dbscan()}}, such as
\code{borderPoints} and nearest-neighbour search controls. The arguments \code{x},
\code{eps}, \code{minPts}, and \code{weights} cannot be supplied through \code{...}.}
}
\value{
An \code{sf} tibble with class \code{hspt_s} and one row per non-noise
cluster. It contains \code{cluster}, the original DBSCAN cluster identifier;
\code{rank}, the priority rank; \code{n}, the number of input point coordinates
intersecting the polygon; \code{prop}, \code{n} as a proportion of all input point
coordinates; and \code{geometry}. Since cluster polygons may overlap, the sum
of \code{prop} can exceed one. Ranking is by decreasing \code{n}, then increasing
polygon area, then increasing cluster identifier.
}
\description{
Identify clusters of points using density-based spatial clustering and
represent each cluster as a buffered convex or concave hull. Clusters are
ranked by the number of points they contain.
}
\details{
When \code{eps = NULL}, the neighbourhood radius is calculated as

\code{sqrt(((min_pts - 1) * A) / (pi * n * density_adjust))},

where \code{A} is the area of the convex hull of the input point coordinates and
\code{n} is the number of coordinates. This means that the expected number of
points in an \code{eps} neighbourhood is approximately \code{density_adjust} times the
mean density of points within the convex hull. A value of
\code{density_adjust = 1} identifies clusters with at least the mean density;
the default of \code{2} requires approximately twice the mean density, and still
larger values identify clusters with higher density.

\code{MULTIPOINT} geometries are cast to individual points before analysis. The
output \code{n} column counts all input point coordinates intersecting each final
cluster polygon, not only points assigned to that DBSCAN cluster. DBSCAN
includes border points in clusters by default; supplying
\code{borderPoints = FALSE} through \code{...} instead performs DBSCAN* and excludes
border points from the points used to construct the hull.

Each cluster geometry is the selected hull of all assigned points, buffered
by \code{eps} and clipped to the convex hull of all input points. Different
cluster polygons may overlap after hull construction and buffering.
}
\examples{
\donttest{
hotspot_dbscan(memphis_robberies_jan)

hotspot_dbscan(
  memphis_robberies_jan,
  density_adjust = 3,
  hull = "convex"
)
}

}
