% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hotspot_isoband.R
\name{hotspot_isoband}
\alias{hotspot_isoband}
\title{Convert a hotspot grid to isobands}
\usage{
hotspot_isoband(
  data,
  value = NULL,
  breaks = 5,
  style = "equal",
  critical_p = 0.05,
  quiet = FALSE
)
}
\arguments{
\item{data}{An \link[sf:sf]{sf::sf} object containing a regular square grid and at least
one numeric column, typically produced by a \verb{hotspot_*()} function.}

\item{value}{The unquoted name of the numeric column to convert. If \code{NULL},
a suitable column is inferred from the sfhotspot class of \code{data}, or from
the sole numeric column in an otherwise unrecognised SF object.}

\item{breaks}{Either a single positive integer giving the requested number
of bands (five by default), or a strictly increasing numeric vector giving
the band boundaries. If multiple boundaries are supplied, \code{style} is
silently ignored.}

\item{style}{Method used to calculate breaks when \code{breaks} has length one.
Values other than \code{"pconventions"} are passed to the \code{style} argument of
\code{\link[classInt:classIntervals]{classInt::classIntervals()}}. \code{"pconventions"} uses two-sided normal-theory
thresholds corresponding to p-values of 0.1, 0.05, 0.01 and 0.001 and
ignores \code{breaks}.}

\item{critical_p}{For output from \code{\link[=hotspot_gistar]{hotspot_gistar()}} containing a \code{kde}
column, the largest p-value for a cell to be included. The default is
\code{0.05}. This argument is ignored for other inputs.}

\item{quiet}{If \code{TRUE}, suppress informative messages about automatically
selected values and potentially unhelpful intervals.}
}
\value{
An \code{sf} tibble with class \code{hspt_ib}. It has one row per non-empty
band and columns \code{lower}, \code{upper}, \code{band}, \code{label}, and \code{geometry}. The
\code{band} and \code{label} columns are ordered factors; \code{band} contains technical
interval notation and \code{label} contains ranges formatted for display.
}
\description{
Generalise values in a regular square grid into polygon bands. The result is
an \link[sf:sf]{sf::sf} object with one row for each non-empty band and can be plotted
with \code{\link[=autoplot]{autoplot()}} or \code{\link[=autolayer]{autolayer()}}.
}
\details{
The default \code{style = "equal"} divides the observed range into equal-width
intervals, providing a discrete analogue of a linear continuous colour
scale. Extreme values can make equal-width bands uninformative; a message is
produced when at least 90\% of finite values fall in one calculated band.

For change values, Gi*/Gi statistics, and dual-KDE differences or logged
ratios, automatically calculated breaks place zero at a boundary or at the
centre of a band. Explicit break vectors are never altered.

When \code{data} is output from \code{\link[=hotspot_gistar]{hotspot_gistar()}} and contains KDE values, KDE
is converted and cells with \code{pvalue >= critical_p} are excluded before the
bands are calculated. Without KDE values, the Gi*/Gi statistic is converted.

Values are interpolated between grid-cell centres. Lattice positions absent
from a grid clipped to its original analysis area are treated as missing.
Consequently, the isobands need not cover the complete area of every outer
grid cell.
}
\examples{
\donttest{
memphis_robberies_jan |>
  hotspot_kde() |>
  hotspot_isoband() |>
  autoplot()
}

}
