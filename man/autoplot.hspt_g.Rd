% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R
\name{autoplot.hspt_g}
\alias{autoplot.hspt_g}
\alias{autolayer.hspt_g}
\title{Plot map of Getis-Ord Gi* results}
\usage{
\method{autoplot}{hspt_g}(object, critical_p = 0.05, sign = c("both", "hot", "cold"), ...)

\method{autolayer}{hspt_g}(object, critical_p = 0.05, sign = c("both", "hot", "cold"), ...)
}
\arguments{
\item{object}{An object with class \code{hspt_g}, e.g. as produced by
\code{\link[=hotspot_gistar]{hotspot_gistar()}}.}

\item{critical_p}{A single numeric value specifying the largest p-value to
treat as statistically significant when plotting density.}

\item{sign}{Which significant results should show density: \code{"both"} (the
default), \code{"hot"} for positive Gi*/Gi values, or \code{"cold"} for negative
values.}

\item{...}{Further arguments passed to \code{\link[ggplot2:geom_sf]{ggplot2::geom_sf()}}, e.g. \code{alpha}.}
}
\value{
\code{autoplot()} returns a \link[ggplot2:ggplot]{ggplot2::ggplot} object. \code{autolayer()}
returns a layer that can be added to a \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
If \code{object} contains a \code{kde} column, density is shown only in cells in which
the Gi*/Gi result passes the specified significance and sign conditions.
The \code{pvalue} column is used as supplied and is not adjusted by the plotting
methods. Cells that do not satisfy the conditions are transparent. If
\code{object} does not contain a \code{kde} column, the Gi*/Gi value is plotted using a
diverging scale centred on zero and \code{critical_p} and \code{sign} do not affect the
mapped values.
}
\section{Functions}{
\itemize{
\item \code{autolayer(hspt_g)}: Create a ggplot layer of Getis-Ord Gi* results.

}}
